<input type="submit"{if is_set($element.options.label)} value="{$element.options.label}" {/if}{foreach $element.available_html_attributes as $attribute}{if is_set($element.options.$attribute)} {$attribute}="{$element.options.$attribute}"{/if}{/foreach} />